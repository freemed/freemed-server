// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: scheduler.sql

package dbgen

import (
	"context"
	"database/sql"
	"time"
)

const schedulerDailyApptRange = `-- name: SchedulerDailyApptRange :many
SELECT s.caldateof AS date_of
, DATE_FORMAT(s.caldateof, '%m/%d/%Y') AS date_of_mdy
, s.calhour AS hour
, s.calminute AS minute
, CONCAT(LPAD(s.calhour, 2, '0'), ':', LPAD(s.calminute, 2, '0')) AS appointment_time
, s.calduration AS duration
, CONCAT(ph.phylname, ', ', ph.phyfname) AS provider
, ph.id AS provider_id
, s.caltype AS resource_type
, CASE s.caltype WHEN 'block' THEN '-' WHEN 'temp' THEN CONCAT('[!] ', ci.cilname, ', ', ci.cifname, ' (', ci.cicomplaint, ')') WHEN 'group' THEN CONCAT(cg.groupname, ' (', cg.grouplength, ' members)') ELSE CONCAT(pa.ptlname, ', ', pa.ptfname, IF(LENGTH(pa.ptmname)>0, CONCAT(' ', pa.ptmname), ''), IF(LENGTH(pa.ptsuffix)>0, CONCAT(' ', pa.ptsuffix), ''), IF(LENGTH(pa.ptid)>0, CONCAT(' (', pa.ptid, ')'), '')) END AS patient
, s.calpatient AS patient_id
, s.calprenote AS note
, SUBSTRING_INDEX(GROUP_CONCAT(st.sname), ',', -1) AS status
, SUBSTRING_INDEX(GROUP_CONCAT(st.scolor), ',', -1) AS status_color
, s.id AS scheduler_id
, s.calappttemplate AS appointment_template_id
, aptm.atcolor AS template_color
FROM scheduler s
LEFT OUTER JOIN appttemplate aptm ON s.calappttemplate = aptm.id
LEFT OUTER JOIN scheduler_status ss ON s.id = ss.csappt
LEFT OUTER JOIN schedulerstatustype st ON st.id = ss.csstatus
LEFT OUTER JOIN physician ph ON s.calphysician = ph.id
LEFT OUTER JOIN patient pa ON s.calpatient = pa.id
LEFT OUTER JOIN callin ci ON s.calpatient = ci.id
LEFT OUTER JOIN calgroup cg ON s.calpatient = cg.id
WHERE ( s.caldateof >= ? AND s.caldateof <= ? )
AND s.calstatus NOT IN ( 'noshow', 'cancelled' )
GROUP BY s.id, ss.csappt
ORDER BY s.caldateof, s.calhour, s.calminute, s.calphysician DESC
`

type SchedulerDailyApptRangeParams struct {
	FromDate time.Time `json:"from_date"`
	ToDate   time.Time `json:"to_date"`
}

type SchedulerDailyApptRangeRow struct {
	DateOf                time.Time      `json:"date_of"`
	DateOfMdy             string         `json:"date_of_mdy"`
	Hour                  int64          `json:"hour"`
	Minute                int64          `json:"minute"`
	AppointmentTime       string         `json:"appointment_time"`
	Duration              int64          `json:"duration"`
	Provider              string         `json:"provider"`
	ProviderID            sql.NullInt64  `json:"provider_id"`
	ResourceType          string         `json:"resource_type"`
	Patient               interface{}    `json:"patient"`
	PatientID             int64          `json:"patient_id"`
	Note                  string         `json:"note"`
	Status                string         `json:"status"`
	StatusColor           string         `json:"status_color"`
	SchedulerID           int64          `json:"scheduler_id"`
	AppointmentTemplateID int64          `json:"appointment_template_id"`
	TemplateColor         sql.NullString `json:"template_color"`
}

func (q *Queries) SchedulerDailyApptRange(ctx context.Context, arg SchedulerDailyApptRangeParams) ([]SchedulerDailyApptRangeRow, error) {
	rows, err := q.db.QueryContext(ctx, schedulerDailyApptRange, arg.FromDate, arg.ToDate)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SchedulerDailyApptRangeRow
	for rows.Next() {
		var i SchedulerDailyApptRangeRow
		if err := rows.Scan(
			&i.DateOf,
			&i.DateOfMdy,
			&i.Hour,
			&i.Minute,
			&i.AppointmentTime,
			&i.Duration,
			&i.Provider,
			&i.ProviderID,
			&i.ResourceType,
			&i.Patient,
			&i.PatientID,
			&i.Note,
			&i.Status,
			&i.StatusColor,
			&i.SchedulerID,
			&i.AppointmentTemplateID,
			&i.TemplateColor,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const schedulerDailyApptRangeByProvider = `-- name: SchedulerDailyApptRangeByProvider :many
SELECT s.caldateof AS date_of
, DATE_FORMAT(s.caldateof, '%m/%d/%Y') AS date_of_mdy
, s.calhour AS hour
, s.calminute AS minute
, CONCAT(LPAD(s.calhour, 2, '0'), ':', LPAD(s.calminute, 2, '0')) AS appointment_time
, s.calduration AS duration
, CONCAT(ph.phylname, ', ', ph.phyfname) AS provider
, ph.id AS provider_id
, s.caltype AS resource_type
, CASE s.caltype WHEN 'block' THEN '-' WHEN 'temp' THEN CONCAT('[!] ', ci.cilname, ', ', ci.cifname, ' (', ci.cicomplaint, ')') WHEN 'group' THEN CONCAT(cg.groupname, ' (', cg.grouplength, ' members)') ELSE CONCAT(pa.ptlname, ', ', pa.ptfname, IF(LENGTH(pa.ptmname)>0, CONCAT(' ', pa.ptmname), ''), IF(LENGTH(pa.ptsuffix)>0, CONCAT(' ', pa.ptsuffix), ''), IF(LENGTH(pa.ptid)>0, CONCAT(' (', pa.ptid, ')'), '')) END AS patient
, s.calpatient AS patient_id
, s.calprenote AS note
, SUBSTRING_INDEX(GROUP_CONCAT(st.sname), ',', -1) AS status
, SUBSTRING_INDEX(GROUP_CONCAT(st.scolor), ',', -1) AS status_color
, s.id AS scheduler_id
, s.calappttemplate AS appointment_template_id
, aptm.atcolor AS template_color
FROM scheduler s
LEFT OUTER JOIN appttemplate aptm ON s.calappttemplate = aptm.id
LEFT OUTER JOIN scheduler_status ss ON s.id = ss.csappt
LEFT OUTER JOIN schedulerstatustype st ON st.id = ss.csstatus
LEFT OUTER JOIN physician ph ON s.calphysician = ph.id
LEFT OUTER JOIN patient pa ON s.calpatient = pa.id
LEFT OUTER JOIN callin ci ON s.calpatient = ci.id
LEFT OUTER JOIN calgroup cg ON s.calpatient = cg.id
WHERE ( s.caldateof >= ? AND s.caldateof <= ? )
AND s.calstatus NOT IN ( 'noshow', 'cancelled' )
AND s.calphysician = ?
GROUP BY s.id, ss.csappt
ORDER BY s.caldateof, s.calhour, s.calminute, s.calphysician DESC
`

type SchedulerDailyApptRangeByProviderParams struct {
	FromDate   time.Time `json:"from_date"`
	ToDate     time.Time `json:"to_date"`
	ProviderID int64     `json:"provider_id"`
}

type SchedulerDailyApptRangeByProviderRow struct {
	DateOf                time.Time      `json:"date_of"`
	DateOfMdy             string         `json:"date_of_mdy"`
	Hour                  int64          `json:"hour"`
	Minute                int64          `json:"minute"`
	AppointmentTime       string         `json:"appointment_time"`
	Duration              int64          `json:"duration"`
	Provider              string         `json:"provider"`
	ProviderID            sql.NullInt64  `json:"provider_id"`
	ResourceType          string         `json:"resource_type"`
	Patient               interface{}    `json:"patient"`
	PatientID             int64          `json:"patient_id"`
	Note                  string         `json:"note"`
	Status                string         `json:"status"`
	StatusColor           string         `json:"status_color"`
	SchedulerID           int64          `json:"scheduler_id"`
	AppointmentTemplateID int64          `json:"appointment_template_id"`
	TemplateColor         sql.NullString `json:"template_color"`
}

func (q *Queries) SchedulerDailyApptRangeByProvider(ctx context.Context, arg SchedulerDailyApptRangeByProviderParams) ([]SchedulerDailyApptRangeByProviderRow, error) {
	rows, err := q.db.QueryContext(ctx, schedulerDailyApptRangeByProvider, arg.FromDate, arg.ToDate, arg.ProviderID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SchedulerDailyApptRangeByProviderRow
	for rows.Next() {
		var i SchedulerDailyApptRangeByProviderRow
		if err := rows.Scan(
			&i.DateOf,
			&i.DateOfMdy,
			&i.Hour,
			&i.Minute,
			&i.AppointmentTime,
			&i.Duration,
			&i.Provider,
			&i.ProviderID,
			&i.ResourceType,
			&i.Patient,
			&i.PatientID,
			&i.Note,
			&i.Status,
			&i.StatusColor,
			&i.SchedulerID,
			&i.AppointmentTemplateID,
			&i.TemplateColor,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const schedulerDailyApptScheduler = `-- name: SchedulerDailyApptScheduler :many
CALL schedulerGenerateDailySchedule(?, ?, ?, ?, ?)
`

type SchedulerDailyApptSchedulerParams struct {
	ReqDate         interface{} `json:"req_date"`
	StartHour       interface{} `json:"start_hour"`
	EndHour         interface{} `json:"end_hour"`
	IntervalMinutes interface{} `json:"interval_minutes"`
	ProviderID      interface{} `json:"provider_id"`
}

type SchedulerDailyApptSchedulerRow struct {
}

func (q *Queries) SchedulerDailyApptScheduler(ctx context.Context, arg SchedulerDailyApptSchedulerParams) ([]SchedulerDailyApptSchedulerRow, error) {
	rows, err := q.db.QueryContext(ctx, schedulerDailyApptScheduler,
		arg.ReqDate,
		arg.StartHour,
		arg.EndHour,
		arg.IntervalMinutes,
		arg.ProviderID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SchedulerDailyApptSchedulerRow
	for rows.Next() {
		var i SchedulerDailyApptSchedulerRow
		if err := rows.Scan(); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const schedulerFindDateAppt = `-- name: SchedulerFindDateAppt :many
SELECT id, created_at, updated_at, deleted_at, caldateof, calcreated, calmodified, caltype, calhour, calminute, calduration, calfacility, calroom, calphysician, calpatient, calcptcode, calstatus, calprenote, calpostnote, calmark, calgroupid, calgroupmembers, calrecurnote, calrecurid, calappttemplate, calattendees, user FROM scheduler
WHERE caldateof = ?
AND calstatus != 'cancelled'
`

func (q *Queries) SchedulerFindDateAppt(ctx context.Context, reqDate time.Time) ([]Scheduler, error) {
	rows, err := q.db.QueryContext(ctx, schedulerFindDateAppt, reqDate)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Scheduler
	for rows.Next() {
		var i Scheduler
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
			&i.Caldateof,
			&i.Calcreated,
			&i.Calmodified,
			&i.Caltype,
			&i.Calhour,
			&i.Calminute,
			&i.Calduration,
			&i.Calfacility,
			&i.Calroom,
			&i.Calphysician,
			&i.Calpatient,
			&i.Calcptcode,
			&i.Calstatus,
			&i.Calprenote,
			&i.Calpostnote,
			&i.Calmark,
			&i.Calgroupid,
			&i.Calgroupmembers,
			&i.Calrecurnote,
			&i.Calrecurid,
			&i.Calappttemplate,
			&i.Calattendees,
			&i.User,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const schedulerFindDateApptByProvider = `-- name: SchedulerFindDateApptByProvider :many
SELECT id, created_at, updated_at, deleted_at, caldateof, calcreated, calmodified, caltype, calhour, calminute, calduration, calfacility, calroom, calphysician, calpatient, calcptcode, calstatus, calprenote, calpostnote, calmark, calgroupid, calgroupmembers, calrecurnote, calrecurid, calappttemplate, calattendees, user FROM scheduler
WHERE caldateof = ?
AND calstatus != 'cancelled'
AND calphysician = ?
`

type SchedulerFindDateApptByProviderParams struct {
	ReqDate    time.Time `json:"req_date"`
	ProviderID int64     `json:"provider_id"`
}

func (q *Queries) SchedulerFindDateApptByProvider(ctx context.Context, arg SchedulerFindDateApptByProviderParams) ([]Scheduler, error) {
	rows, err := q.db.QueryContext(ctx, schedulerFindDateApptByProvider, arg.ReqDate, arg.ProviderID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Scheduler
	for rows.Next() {
		var i Scheduler
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
			&i.Caldateof,
			&i.Calcreated,
			&i.Calmodified,
			&i.Caltype,
			&i.Calhour,
			&i.Calminute,
			&i.Calduration,
			&i.Calfacility,
			&i.Calroom,
			&i.Calphysician,
			&i.Calpatient,
			&i.Calcptcode,
			&i.Calstatus,
			&i.Calprenote,
			&i.Calpostnote,
			&i.Calmark,
			&i.Calgroupid,
			&i.Calgroupmembers,
			&i.Calrecurnote,
			&i.Calrecurid,
			&i.Calappttemplate,
			&i.Calattendees,
			&i.User,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const schedulerGetEvent = `-- name: SchedulerGetEvent :one
SELECT s.caldateof AS date_of
, DATE_FORMAT(s.caldateof, '%m/%d/%Y') AS date_of_mdy
, s.calhour AS hour
, s.calminute AS minute
, CONCAT(LPAD(s.calhour, 2, '0'), ':', LPAD(s.calminute, 2, '0')) AS appointment_time
, s.calduration AS duration
, CONCAT(ph.phylname, ', ', ph.phyfname) AS provider
, ph.id AS provider_id
, s.caltype AS resource_type
, CASE s.caltype WHEN 'block' THEN '-' WHEN 'temp' THEN CONCAT('[!] ', ci.cilname, ', ', ci.cifname, ' (', ci.cicomplaint, ')') WHEN 'group' THEN CONCAT(cg.groupname, ' (', cg.grouplength, ' members)') ELSE CONCAT(pa.ptlname, ', ', pa.ptfname, IF(LENGTH(pa.ptmname)>0, CONCAT(' ', pa.ptmname), ''), IF(LENGTH(pa.ptsuffix)>0, CONCAT(' ', pa.ptsuffix), ''), IF(LENGTH(pa.ptid)>0, CONCAT(' (', pa.ptid, ')'), '')) END AS patient
, s.calpatient AS patient_id
, s.calprenote AS note
, SUBSTRING_INDEX(GROUP_CONCAT(st.sname), ',', -1) AS status
, SUBSTRING_INDEX(GROUP_CONCAT(st.scolor), ',', -1) AS status_color
, s.id AS scheduler_id
, s.calappttemplate AS appointment_template_id
, aptm.atcolor AS template_color
FROM scheduler s
LEFT OUTER JOIN appttemplate aptm ON s.calappttemplate = aptm.id
LEFT OUTER JOIN scheduler_status ss ON s.id = ss.csappt
LEFT OUTER JOIN schedulerstatustype st ON st.id = ss.csstatus
LEFT OUTER JOIN physician ph ON s.calphysician = ph.id
LEFT OUTER JOIN patient pa ON s.calpatient = pa.id
LEFT OUTER JOIN callin ci ON s.calpatient = ci.id
LEFT OUTER JOIN calgroup cg ON s.calpatient = cg.id
WHERE s.id = ?
`

type SchedulerGetEventRow struct {
	DateOf                time.Time      `json:"date_of"`
	DateOfMdy             string         `json:"date_of_mdy"`
	Hour                  int64          `json:"hour"`
	Minute                int64          `json:"minute"`
	AppointmentTime       string         `json:"appointment_time"`
	Duration              int64          `json:"duration"`
	Provider              string         `json:"provider"`
	ProviderID            sql.NullInt64  `json:"provider_id"`
	ResourceType          string         `json:"resource_type"`
	Patient               interface{}    `json:"patient"`
	PatientID             int64          `json:"patient_id"`
	Note                  string         `json:"note"`
	Status                string         `json:"status"`
	StatusColor           string         `json:"status_color"`
	SchedulerID           int64          `json:"scheduler_id"`
	AppointmentTemplateID int64          `json:"appointment_template_id"`
	TemplateColor         sql.NullString `json:"template_color"`
}

func (q *Queries) SchedulerGetEvent(ctx context.Context, id int64) (SchedulerGetEventRow, error) {
	row := q.db.QueryRowContext(ctx, schedulerGetEvent, id)
	var i SchedulerGetEventRow
	err := row.Scan(
		&i.DateOf,
		&i.DateOfMdy,
		&i.Hour,
		&i.Minute,
		&i.AppointmentTime,
		&i.Duration,
		&i.Provider,
		&i.ProviderID,
		&i.ResourceType,
		&i.Patient,
		&i.PatientID,
		&i.Note,
		&i.Status,
		&i.StatusColor,
		&i.SchedulerID,
		&i.AppointmentTemplateID,
		&i.TemplateColor,
	)
	return i, err
}
